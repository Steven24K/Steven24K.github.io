{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "This is my personal website blog and portfolio. Made with docusaurus",
  "scripts": {
    "examples": "docusaurus-examples",
    "start": "docusaurus-start",
    "build": "docusaurus-build",
    "deploy": "docusaurus-build && gh-pages -b master -d build/Portfolio",
    "deploy-without-build": "gh-pages -b master -d build/Portfolio",
    "write-translations": "docusaurus-write-translations",
    "version": "docusaurus-version",
    "rename-version": "docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.3.0"
  },
  "author": "Steven K",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^1.2.0"
  }
}
